import { Box } from "@mui/material";

export default function MUIBox() {
  return (
    <Box sx={{ padding: 2, backgroundColor: "grey.500" }}>
      This is a Box with padding and background.
    </Box>
  );
}
